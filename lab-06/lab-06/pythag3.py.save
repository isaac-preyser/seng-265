#!/usr/bin/env python3

import sys
import math

def pythag(a=0.0, b=0.0):
	return math.sqrt(a**2 + b**2)


def main():
 
    # parse the command-line arguments return 
    # sqrt(a^2 + b^2)print an error and quit 
    # if there aren't the right number

    # convert the command line arguments from strings to floats

    

    print("Sides ", a, " and ", b, ", hypotenuse ", end="", sep="")
    print("%.4f" % pythag(a, b))


if __name__ == "__main__":
    main()
